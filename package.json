{
  "name": "lumen-x-deli-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Lumen x Deli - High-End 3D Printed Lamps & Drone Parts Ecommerce",
  "workspaces": [
    "shared",
    "client",
    "server"
  ],
  "scripts": {
    "dev:client": "npm run dev -w client",
    "dev:server": "npm run dev -w server",
    "build:client": "npm run build -w client",
    "build:server": "npm run build -w server",
    "build:shared": "npm run build -w shared",
    "build:all": "npm run build -w shared && npm run build -w server && npm run build -w client"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
